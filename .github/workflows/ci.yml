name: CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test-backend:
    name: Test Backend
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'
    
    - name: Install dependencies
      working-directory: ./backend
      run: npm install
    
    - name: Run linter (if configured)
      working-directory: ./backend
      run: npm run lint || echo "No linter configured, skipping..."
      continue-on-error: true
    
    - name: Run tests (if configured)
      working-directory: ./backend
      run: npm test || echo "No tests configured, skipping..."
      continue-on-error: true
  
  test-frontend:
    name: Test Frontend
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'
    
    - name: Install dependencies
      working-directory: ./frontend
      run: npm install
    
    - name: Run linter (if configured)
      working-directory: ./frontend
      run: npm run lint || echo "No linter configured, skipping..."
      continue-on-error: true
    
    - name: Build
      working-directory: ./frontend
      run: npm run build
  
  build-docker:
    name: Build Docker Images
    runs-on: ubuntu-latest
    needs: [test-backend, test-frontend]
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    
    - name: Build Backend Image
      uses: docker/build-push-action@v4
      with:
        context: ./backend
        push: false
        tags: hytale-web-manager-backend:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max
    
    - name: Build Frontend Image
      uses: docker/build-push-action@v4
      with:
        context: ./frontend
        push: false
        tags: hytale-web-manager-frontend:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max
    
    - name: Build Hytale Wrapper Image
      uses: docker/build-push-action@v4
      with:
        context: ./hytale-server-wrapper
        push: false
        tags: hytale-wrapper:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max
  
  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: 'fs'
        scan-ref: '.'
        format: 'sarif'
        output: 'trivy-results.sarif'
      continue-on-error: true
    
    - name: Upload Trivy results to GitHub Security tab
      uses: github/codeql-action/upload-sarif@v2
      with:
        sarif_file: 'trivy-results.sarif'
      continue-on-error: true
